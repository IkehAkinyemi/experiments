#include <stdio.h>
#include <stdlib.h>

// Node structure.
struct Node
{
 int data;
 struct Node* next;
};

// Function to insert a node at the beginning of the list.
void insertAtBeginning(struct Node** head) {

}
